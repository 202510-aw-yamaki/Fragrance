# 2026年3月8日 17時25分 作業メモ

## 対象
- `香りワークショップ_トップページ.html`（現 `index.html`）
- 参照元: `香りワークショップ_トップページ大元.html`

## 本日の進捗

### 1. 文字化け・保存形式まわりの復旧
- `香りワークショップ_トップページ.html`（現 `index.html`）を UTF-8 で扱う前提に整理
- BOM 起因の問題を都度修正し、現在は通常編集可能な状態
- 文字化け復旧の過程で崩れていたHTML構造を段階的に修正

### 2. 大元ファイルとの整合回復
- `香りワークショップ_トップページ大元.html` を参照し、抜けていたCSS定義を補完
- `hero` と `question-preview` を中心に現行要件を残しつつ、その他のエリアは大元寄りに整理
- `section-inner` 運用も復帰済み

### 3. ヘッダーの整理
- ヘッダーCTAは削除済み
- ナビ項目は短い表現に変更済み
  - `TOP`
  - `流れ`
  - `質問`
  - `価値`
  - `安心`
  - `お願い`
- `お願い` は Final Call To Action エリアへスクロール
- `TOP` と `上へ戻る` は `#top` を利用
- 固定ヘッダー配慮として `scroll-padding-top` を追加

### 4. Hero エリア調整
- `hero-wrap` 内のコピーとビジュアル配置を調整
- `hero-wrap` 下に 3カラムの `hero-gallery` プレースホルダーを確保
  - アンケート画面イメージ予定
  - グラフイメージ予定
  - スライダー画面イメージ予定
- `hero-wrap` と `hero-gallery` 間に 30px の余白を追加
- レスポンシブ条件は「幅768px以上」または「16:9以上の横長」でPC配置を使う方針に寄せて調整

### 5. Question Page Preview の改善
- `Question Page Preview` をトップページ上の本物らしいプレビューとして再構成
- PC版プレビューを、左ナビ + メイン設問 + 補助情報の見え方に近づける方向で修正
- `preview-layout` は PC 時に左カラム優先の比率へ調整
- `preview-shell preview-pc` 内に CTA を配置
- セクション下の重複CTAは削除
- 各プレビュー内に以下のHTMLコメントを追加済み
  - `実際は各画面が完成した場合は画像に差し替える`

### 6. 回答後プレビューの再実装
- 以前消えていた「レーダー風グラフ + 5スライダー」プレビューを復帰
- 構成は「グラフが上 / スライダーが下」に変更
- SVGグラフとスライダーの簡易連動デモを維持
- 香りバランスが固定ではなく、あとから微調整できる見せ方を反映

### 7. Experience Value の調整
- `benefits-grid` を 3要素構成へ変更
- 左端に画像プレースホルダーを追加
- 右側に価値カードを並べる構成へ更新

### 8. Reassurance の調整
- 右側が単なる `div` だった状態を修正し、左側同様 `card` に統一
- FAQ4項目はすべて常時表示に変更
- `8問からはじめる` CTA を安心情報カード内へ移動
- セクション下部の重複CTAは削除

### 9. Final Call To Action の調整
- CTAエリアを2カラム構成に変更
- `cta-actions` の右側に画像プレースホルダーを追加
- `お願い` ナビから到達する終点として使える構造に整理

## 現在の状態
- 主要な導線は成立している
- ヘッダー、Hero、Question Preview、Reassurance、Final CTA は一通り要件反映済み
- トップページとしての見せ方はかなり整ってきている
- ただし、まだ「仮のUI」や「プレースホルダー」の部分が多く、詰め切り余地がある

## 次回以降に継続すべき修正点

### 優先度高
1. `Question Page Preview` のPC版を、資料のワイヤーフレーム3ページ目にさらに寄せる
- 左ナビの見せ方
- 中央の回答パネル密度
- 右側の補助領域の情報整理
- 実画像に置き換える前提で、HTMLモックの完成度を上げる

2. `Question Page Preview` のスマホ版の完成度向上
- 1問1画面の集中感をさらに強める
- 余白、文字サイズ、選択肢サイズを実機寄りに微調整

3. `#top` スクロール挙動の最終確定
- 現状は `scroll-padding-top` で対処中
- まだ「最上部ぴったり」に見えない場合は、`href="#top"` ではなく JS で `window.scrollTo({ top: 0, behavior: 'smooth' })` に変える余地あり

### 優先度中
4. プレースホルダー画像の実画像差し替え
- Hero の 3カラム画像
- Experience Value 左画像
- Final CTA 右画像
- Question Preview 内の最終的な画面画像

5. レスポンシブ条件の再点検
- 現在は「幅768px以上」または「16:9以上」でPC配置寄り
- 実機幅で見たときに、Hero / Preview / CTA が同じルール感で崩れるか確認が必要
- 特にタブレット横向きでの見え方を要確認

6. ヘッダーのモバイル体験改善
- 項目名は短縮済みだが、さらに横幅が厳しい場合はモバイル専用導線を検討
- 例: 横スクロールのまま行くか、メニュー化するかの判断

### 優先度低
7. 文言のトーン最終調整
- 現状でも「診断」より「準備」寄りだが、コピーの粒度はさらに整えられる
- Hero と Final CTA の文言を、より一貫したブランドトーンに寄せる余地あり

8. CSSの整理
- 追記修正が多かったため、セクションごとの責務整理がまだ甘い可能性あり
- 安定版にするなら、Hero / Preview / Reassurance / CTA のCSSブロックを軽く再編したほうが保守しやすい

## 更新余地
- 実画像差し替え後に余白・比率は再調整したほうがよい
- PC版プレビューは、最終的にHTMLモックより画像化した方がデザイン再現性が上がる可能性が高い
- 逆に、レーダーグラフ + スライダーはデモとして動く価値があるため、HTML/SVGのまま残す判断も合理的
- CTAは現状十分あるが、スクロール位置ごとの押しやすさは実機確認推奨

## 備考
- 現在のファイルは編集継続前提の途中完成版
- 次回は「見た目の完成度を上げる作業」と「最終的な画像差し替え作業」が中心になる見込み
