# 2026-03-11 10:41 今後作業整理と進行管理

## 目的

- 現在の実装状況を踏まえ、次に行うべき作業を整理する。
- 進行状況をチェック形式で管理できるようにする。
- 作業中に発生した要望、問題点、判断メモを一元管理する。

## 現在の前提整理

- トップページ `香りワークショップ_トップページ.html` は完成扱い。
- `questionnaire.html` はPC版、スマホ版ともに確認と調整が完了し、完成扱いとする。
- `questionnaire_step2.html` は `questionnaire.html` と同方針のレスポンシブ対応を反映済みで、完成扱いとする。
- `fragrance-graph.html` はデモ前提ページとして一通りのレイアウト調整を終え、仮完成扱いとする。
- `reservation.html` はデモ前提ページとしてレイアウト調整を終え、完成扱いとする。
- STEP間連携とグラフ連携は最終的に Spring Boot + DB で実装予定だが、現段階では未実装のままとする。
- 現在はデモンストレーションページとして、まずフロントエンドの完成を優先する。
- 次の主作業対象は `reservation-complete.html` と導線整理。
- `reservation.html` は実データ連携とアクセシビリティ改善を除き、画面調整は完了した。
- 全体進行としては `2026-03-10_1030_実装進行ログ.md` 上で Step 1 完了、Step 2 以降未着手。

## 次に実施すべき作業

### 優先度高

- [ ] 予約完了ページ `reservation-complete.html` の表示と導線確認
- [ ] `reservation-complete.html` と `reservation.html` 間の導線整理
- [ ] 予約完了後の全体導線確認

### 優先度中

- [ ] `reservation.html` の仮実装整理
- [ ] 予約完了ページ `reservation-complete.html` のトーンと導線を確認
- [ ] `questionnaire.html` の調整内容を別Markdownに整理
- [ ] `questionnaire.html` と `questionnaire_step2.html` の重複スタイル、重複スクリプトの整理方針を検討
- [ ] Spring Boot + DB 連携を行う際のフロント受け口を整理

### 優先度低

- [ ] 画像、`alt`、補助文言、仮文言の見直し
- [ ] インライン `style` / `script` の分離方針を検討
- [ ] CSS とセクション責務の整理

## 作業フェーズ別チェック

### Phase 1: questionnaire.html 実画面確認と調整

- [x] PCで `side-panel` の高さ配分を確認
- [x] PCで `side-panel` のはみ出しを解消
- [x] スマホで不要要素の非表示方針を確定
- [x] スマホで選択肢カードの情報量圧縮を実施
- [x] スマホでヘッダーボタン表示を矢印アイコン化
- [x] スマホで戻る導線を `question-shell` 下の専用カードへ整理
- [x] `questionnaire.html` の調整結果を確定

### Phase 2: STEP間データ連携

- [ ] STEP1 の回答保存方式を決める
- [ ] STEP2 への回答引き継ぎを実装する
- [ ] グラフページで回答内容を参照できるようにする
- [ ] 予約ページで必要なデータ参照方針を決める
- [ ] トップへ戻る時の保存済み回答の扱いを整理する

### Phase 3: グラフと予約導線の整理

- [x] `fragrance-graph.html` のデモ前提レイアウト方針を整理する
- [x] `fragrance-graph.html` の最終表示確認を行い、仮完成扱いにする
- [x] `reservation.html` の画面調整を完了する
- [ ] `reservation.html` の `alert` 仮実装を置き換える
- [ ] モーダルのアクセシビリティ改善点を整理する
- [ ] 予約完了導線までの流れを確認する

## 要望メモ

### ユーザー要望

- HTMLファイルの文字化けは、ユーザーから指摘がない限り実ファイル破損ではなく、PowerShell出力時のエンコーディング由来として扱う。
- `questionnaire.html` は今回の調整をもって完成扱いとする。
- `questionnaire_step2.html` も今回の調整をもって完成扱いとする。
- STEP間連携と `fragrance-graph.html` へのデータ連携は、最終的に Spring Boot の DB を使用して実装予定。
- ただし現段階では連携実装は行わず、まずはデモページとしてフロントを完成させる。
- 回答は現段階では一期一会扱いとし、グラフページから質問ページへ戻って参照する導線は持たせない。

### 実装上の判断メモ

- トップページは完成扱いのため、原則大きく触らない。
- `questionnaire.html` は「ヘッダー構造は維持」「レスポンシブだけ再設計」が前提。
- `questionnaire.html` のスマホ版は、情報量を圧縮し、ヘッダーと設問本体を優先する構成に整理した。
- スマホ版の戻る導線は `side-panel` の流用ではなく、`question-shell` 下に置く専用カードとして分離した。
- `questionnaire_step2.html` も同じレスポンシブ方針で統一し、Q6時のみヘッダー左から STEP1 に戻れる分岐をJSで持たせた。
- `fragrance-graph.html` は現段階ではデモページ扱いとし、回答参照やDB保存を前提にしない。
- `fragrance-graph.html` では「質問に戻る」導線を削除し、予約導線へ寄せる。
- `fragrance-graph.html` の中間帯は、グラフカードとスライダーカードを横並びにしつつ、情報量が多い補助領域は非表示で整理する。
- `fragrance-graph.html` は今回のスマホ調整をもって、現段階では仮完成扱いとする。
- `reservation.html` は PC を基準に、中間帯、スマホ帯の順でレイアウトを整理した。
- `reservation.html` は今回の調整をもって、現段階では完成扱いとする。
- 作業中に中間帯のCSSを一度戻し過ぎたため、今後は中間帯メディアクエリの修正時に意図した要件を都度確認しながら進める。
- 次の作業は `reservation-complete.html` の表示確認と導線整理を優先する。

## 問題点メモ

### 現時点で把握している問題

- `fragrance-graph.html` は固定高前提のため、実機差異で再調整が必要になる可能性がある。
- `fragrance-graph.html` の中間帯は調整項目が多く、意図しない巻き戻りが起きやすいため注意が必要。
- 回答保持と画面間データ連携は未実装。ただし現段階では仕様として保留し、フロント完成を優先する。
- `reservation.html` には仮導線、アクセシビリティ面の未解決課題がある。
- 文字化けに見える箇所は、ユーザー環境で再現していない限り、実ファイル破損ではなく PowerShell 出力時のエンコーディング由来として扱う。

### 新たに見つかった問題の追記欄

- ここに都度追記

## 作業ログ

### 2026-03-11 10:41:53

- 現状確認結果をもとに、今後の作業整理と進行管理用Markdownを新規作成。
- 直近作業は `questionnaire.html` の中間帯・スマホ確認を最優先とする方針で整理。

### 2026-03-11

- `questionnaire.html` のPC版 `side-panel` の高さ配分を見直し、画像 + 2カード構成が下にはみ出しにくい形へ修正。
- `questionnaire.html` のスマホ版で `step-strip`、`nav-panel`、`side-panel`、`question-caption`、`helper-text`、`option-desc` を非表示化。
- スマホ版ヘッダーの `前の質問` / `次の質問` を Material Icons の `arrow_back` / `arrow_forward` に変更し、中央揃えに調整。
- スマホ版の選択肢カードは `option-label` と `option-title` を同一行に再配置し、`option-title` は1行表示優先の可変サイズに調整。
- スマホ版の戻る導線は `question-shell` 下の専用カード `mobile-return-card` として再構成し、ボタン下に注意文を配置。
- スマホ版で `question-shell` と戻るカードの左右余白を統一し、不要になった `body` 下部余白を削除。
- 本作業をもって `questionnaire.html` は完成扱いとする。
- `questionnaire_step2.html` に `questionnaire.html` と同方針のレスポンシブ対応を展開。
- `questionnaire_step2.html` のPC版 `side-panel` の高さ配分を見直し、画像 + 2カード構成のはみ出しを抑える形に修正。
- `questionnaire_step2.html` のスマホ版で `step-strip`、`nav-panel`、`side-panel`、`question-caption`、`helper-text`、`option-desc` を非表示化。
- `questionnaire_step2.html` のスマホ版ヘッダーは Material Icons の `arrow_back` / `arrow_forward` 表示に変更し、スマホ専用の戻るカード `mobile-return-card` を追加。
- `questionnaire_step2.html` の選択肢カードは、ラベル横タイトル化、1行表示優先の可変サイズ化を反映。
- `questionnaire_step2.html` のヘッダー左ボタンは、Q6の時だけ `STEP1に戻る` 表示に切り替え、別ページでデータ持越し不可の確認ポップアップを出すように調整。
- 本作業をもって `questionnaire_step2.html` は完成扱いとする。
- `fragrance-graph.html` はデモページ前提とし、現段階では回答参照やDB連携を行わない方針を確定。
- `fragrance-graph.html` から「質問に戻る」ボタンを削除し、予約導線中心のCTAへ整理。
- `fragrance-graph.html` のCTA文言によるボタンはみ出しを抑えるため、文言改行依存を外し、ボタン幅とCTAレイアウトを調整。
- `fragrance-graph.html` のレスポンシブを `PC / 中間帯 / SP` の3段階に整理し、旧 `16:9` 条件によるPC化を撤廃。
- `fragrance-graph.html` のPC版で `cta-box` のコピー領域と画像プレースホルダー領域を 1:1 幅に調整。
- `fragrance-graph.html` の中間帯では、グラフカードとスライダーカードを横並びにし、`helper-grid` とスライダー数値表示を非表示化。
- `fragrance-graph.html` の中間帯では、`id="cta"` 内のプレースホルダーを非表示化し、CTAテキスト側のみ表示する構成に整理。
- `fragrance-graph.html` の `slider-actions` は4ボタンを常時横並びにし、ボタン幅を均一化、フォントは可変指定へ調整。
- `fragrance-graph.html` のスマホ版で `graph-adjustment` 見出し補助文、`header-note`、CTA下部の画像プレースホルダーを非表示化。
- `fragrance-graph.html` のスマホ版で `graph-adjustment` の上下余白を縮小し、`#cta` 下部余白を `10px` に調整。
- `fragrance-graph.html` のスマホ版でスライダー行の数値表示を非表示化し、中間帯寄りの情報量に整理。
- `fragrance-graph.html` のヘッダー右側を予約ボタンに置き換え、スマホでも1行で収まる構成に調整。
- `fragrance-graph.html` のヘッダーブランドリンクはトップページ戻りに変更し、「現在の調整は保存されない」確認ダイアログを追加。
- 本作業をもって `fragrance-graph.html` は仮完成扱いとする。
- `reservation.html` のブレークポイントを `PC / 中間帯 / SP` の3段階に統一し、`html` / `body` に `min-width: 375px` を設定。
- `reservation.html` のPC版では、`summary-panel` と `booking-panel` の下に `memo-panel` が来るようにグリッドを再構成し、ヘッダー右上リンクと `step-strip` を削除した。
- `reservation.html` のPC版では、予約確定ボタンをヘッダー右側へ移設し、`side-panel` の高さ・余白・カード配置を調整した。
- `reservation.html` の予約情報カードでは、`来店情報` 見出し追加、`来店内容` / `人数` ラベルの軽量化、`4名以上` 選択肢追加、メモ欄補助文更新を行った。
- `reservation.html` のモーダルは横幅と高さ上限を見直し、1日1行レイアウト、空き状況チップ、担当講師チップ、講師性別別カラーの仮データ表示に変更した。
- `reservation.html` の中間帯では `side-panel` を非表示にし、左上に `summary-panel`、右上に `booking-panel`、下段全面に `memo-panel` を配置した。
- `reservation.html` のスマホ帯では `summary-panel`、`side-panel`、`booking-intro` を非表示にし、人数選択後にメモ欄へ自動スクロールする導線を追加した。
- `reservation.html` のメモ欄には `Message for Staff` ラベルを追加した。
- 本作業をもって `reservation.html` は完成扱いとする。

## 進捗サマリー

- [x] 現状確認
- [x] 次作業の整理
- [x] `questionnaire.html` 実画面確認
- [x] `questionnaire_step2.html` 展開
- [x] `fragrance-graph.html` レイアウト整理
- [x] `fragrance-graph.html` 仮完成
- [x] `reservation.html` 完成
- [ ] データ連携実装
- [ ] 予約導線改善
- [ ] 品質改善と整理
