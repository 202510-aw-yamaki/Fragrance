# Spring Boot 実装方針と依存関係

## 対象プロジェクト
- プロジェクト名: Fregrance
- Build Tool: Maven
- Language: Java
- Spring Boot: 3.5.11
- Packaging: Jar
- Java: 21

## 前提
- 既存の `index.html` から `reservation-complete.html` までの静的プロトタイプを、Spring Boot 上で段階的に置き換える。
- 画面構成と導線は現行フロントを踏襲し、まずは予約保存と予約枠取得を接続する。
- 公開導線は匿名アクセスのまま維持し、スタッフ画面のみ認証対象とする。

## 必要な依存関係
```xml
<dependencies>
  <dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-web</artifactId>
  </dependency>
  <dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-thymeleaf</artifactId>
  </dependency>
  <dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-validation</artifactId>
  </dependency>
  <dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-data-jpa</artifactId>
  </dependency>
  <dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-security</artifactId>
  </dependency>
  <dependency>
    <groupId>com.mysql</groupId>
    <artifactId>mysql-connector-j</artifactId>
    <scope>runtime</scope>
  </dependency>
  <dependency>
    <groupId>org.projectlombok</groupId>
    <artifactId>lombok</artifactId>
    <optional>true</optional>
  </dependency>
  <dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-devtools</artifactId>
    <scope>runtime</scope>
    <optional>true</optional>
  </dependency>
  <dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-test</artifactId>
    <scope>test</scope>
  </dependency>
  <dependency>
    <groupId>org.springframework.security</groupId>
    <artifactId>spring-security-test</artifactId>
    <scope>test</scope>
  </dependency>
</dependencies>
```

## 依存関係の採用理由
- `spring-boot-starter-web`: 公開ページ返却と予約API実装の基盤。
- `spring-boot-starter-thymeleaf`: 既存HTMLをテンプレートへ移しやすくするため。
- `spring-boot-starter-validation`: `ReservationForm` と `QuestionnaireForm` の入力検証用。
- `spring-boot-starter-data-jpa`: 予約、質問回答、香りプロフィール、予約枠の永続化用。
- `spring-boot-starter-security`: スタッフログインと管理画面制御用。
- `mysql-connector-j`: 本番想定DB接続用。
- `lombok`: Entity / DTO / Form の記述量削減用。
- `spring-boot-devtools`: ローカル開発効率向上用。
- `spring-boot-starter-test`, `spring-security-test`: Controller / Service / Security のテスト用。

## 今後の作業方針
1. Spring Initializr で Maven / Java 21 / Spring Boot 3.5.11 の土台を生成する。
2. `templates` と `static` に既存フロント資産を配置し、トップページから完了画面までをまず表示可能にする。
3. `ReservationForm`、`QuestionnaireForm`、DTO、Entity を作成し、画面データの受け渡し形を固定する。
4. `ReservationController` と `ReservationSlotService` を先行実装し、予約枠一覧取得 API と予約保存 API を作る。
5. `Reservation`、`QuestionnaireAnswer`、`FragranceProfile`、`ReservationSlot` のテーブルを作成し、JPA で接続する。
6. フロントの `sessionStorage` 依存を段階的に減らし、API 応答で完了画面を描画する構成へ移す。
7. スタッフログイン、予約一覧、予約詳細の順で管理画面を追加する。
8. バリデーション、例外処理、重複予約防止、テストを整備してから画面調整へ戻る。

## 実装順の推奨
- 第1段階: Spring Boot プロジェクト生成、画面表示確認
- 第2段階: 予約枠取得 API
- 第3段階: 予約保存 API
- 第4段階: DB 接続と永続化
- 第5段階: スタッフ画面と認証
- 第6段階: テスト、自動化、微調整

## 注意点
- 公開導線は会員登録なしのまま維持し、離脱を増やさない。
- 予約保存はトランザクションで扱い、枠確保と予約作成を分離しない。
- `sessionStorage` は移行期間の補助に留め、正式データの保存先にはしない。
- Controller に計算ロジックを寄せず、Service へ集約する。
- 画像・CSS・JS のパスは Thymeleaf 化しても崩れないよう先に整理する。
