# 2026-03-16 14:00 午後作業文

## 1. このファイルの目的
- 2026-03-16 午後以降の Spring Boot 実装作業を、設計資料と方針書に沿って進めるための進捗チェックリストとして使う
- `p-answer_0316` 内の設計資料、`2026-03-16_1228_SpringBoot実装方針と依存関係.md`、`2026-03-16_1305_インシデント対応と作業前確認マニュアル.md` を確認した前提で、実装順を固定する
- ポートフォリオ成果物として必要な「設計資料」「Spring Boot 実装」を抜けなく管理する

## 2. 状態表記
- `[〇]` 完了
- `[△]` 進行中
- `[ ]` 未着手
- `[未]` 未実装
- `[保]` 保留
- `[変]` 仕様変更

## 3. 今回の前提確認
- [〇] `p-answer_0316/docs/07-specification.html` および `07-specification-answer.md` を、仕様書の基準資料として扱う
- [〇] `p-answer_0316/docs/08-db-design.html` を、DB設計書の基準資料として扱う
- [〇] `p-answer_0316/docs/09-test-report.html` を、テスト報告書の基準資料として扱う
- [〇] `p-answer_0316/design/class-diagram.html` を、実装時のクラス構成基準として扱う
- [〇] `2026-03-16_1228_SpringBoot実装方針と依存関係.md` の実装順を採用する
- [〇] `2026-03-16_1305_インシデント対応と作業前確認マニュアル.md` の注意点を確認した
- [〇] 公開導線は匿名アクセスのまま維持し、スタッフ画面のみ認証対象とする方針を確認した
- [〇] GitHub リポジトリ作成と PUSH はユーザー側で行う前提を確認した
- [〇] README の最終整備はユーザー側で実施する前提を確認した

## 4. 最終成果物の整理
### 4-1. 設計資料
- [〇] 仕様書
- [〇] DB設計書
- [〇] テスト報告書

### 4-2. バックエンド実装
- [〇] Spring Boot プロジェクト一式
- [〇] Controller
- [〇] Service
- [〇] Repository / Mapper
- [〇] Form / DTO / Model
- [△] Security
- [〇] テストコード

### 4-3. リポジトリ整備
- [保] `README.md` の最終整備はユーザー側で実施
- [保] GitHub リポジトリ作成
- [保] GitHub への PUSH

## 5. 実装の大方針
- [〇] 静的プロトタイプをそのまま捨てず、Spring Boot の `templates` / `static` に段階移植する
- [〇] まずは画面表示成立、その後に API、DB、認証へ進む
- [〇] `sessionStorage` は移行期間の補助として扱い、正式保存先にはしない
- [〇] Controller に業務ロジックを寄せず、Service に集約する
- [〇] SQL は MyBatis Mapper に寄せる
- [〇] 差分更新以外では、既存ファイルの責務と共通資産かどうかを先に確認してから編集する

## 6. 午後作業チェックリスト
### 6-1. 実装前整理
- [〇] Spring Boot 用のディレクトリ構成を決める
- [〇] パッケージ構成を `controller / service / mapper / model / form / dto / security / validation` で整理する
- [〇] 既存 HTML / CSS / JS / 画像の移行対象を洗い出す
- [変] `sessionStorage` 依存箇所を一覧化する
  → 2026-03-26 時点では STEP1 / STEP2 は `sessionStorage` 保持、graph 以降は API 連携開始済みという整理に変わったため、最新状態は `作業md/2026-03-26_1048_バックエンド実態確認と実装プラン.md` を正とする。
- [〇] 画面返却用 Controller と API Controller の責務を分離して決める

### 6-2. 第1段階: Spring Boot 土台作成
- [〇] Maven / Java 21 / Spring Boot 3.5.11 の土台を作成する
- [〇] `pom.xml` に必要依存関係を定義する
- [〇] アプリ起動確認を行う
- [〇] `templates` / `static` の基本配置を作成する
- [〇] トップページから完了ページまでをテンプレート上で表示できる状態にする

### 6-3. 第2段階: 画面移植
- [〇] `index.html` をテンプレートへ移植する
- [〇] `questionnaire.html` をテンプレートへ移植する
- [〇] `questionnaire_step2.html` をテンプレートへ移植する
- [〇] `fragrance-graph.html` をテンプレートへ移植する
- [〇] `reservation.html` をテンプレートへ移植する
- [〇] `reservation-complete.html` をテンプレートへ移植する
- [△] CSS / JS / 画像パスを Spring Boot 配置前提で整理する
- [ ] Thymeleaf 化で崩れないか確認する

### 6-4. 第3段階: ドメインと入出力定義
- [〇] `ReservationForm` を作成する
- [変] `QuestionnaireForm` を作成する
  → 現在は `QuestionnaireResultForm` を採用している。
- [ ] `ReservationModel` を作成する
- [変] `QuestionnaireAnswerModel` を作成する
  → 現在は `QuestionnaireResult` モデルへ整理している。
- [変] `FragranceProfileModel` を作成する
  → 現在は独立モデルを作らず、アンケート結果保存と予約連携を優先する構成へ変わっている。
- [〇] `ReservationSlotModel` を作成する
- [ ] `StaffUserModel` を作成する
- [〇] `ReservationResponse` など API 応答 DTO を作成する

### 6-5. 第4段階: 予約枠取得 API
- [〇] `ReservationSlotController` 相当の API を実装する
- [〇] 日付別の予約枠取得処理を実装する
- [変] モック予約枠を API 応答へ置き換える
  → 2026-03-26 時点ではモック返却を廃止し、DB 実データ返却と空日の空配列返却へ変更済み。
- [〇] 画面側から予約枠 API を呼び出す

### 6-6. 第5段階: 予約保存 API
- [〇] `ReservationController#createReservation` を実装する
- [〇] 入力バリデーションを実装する
- [〇] 予約保存時の共通エラーレスポンスを実装する
- [〇] 完了画面を API / サーバーデータ基準で描画できるようにする
- [△] `reservation.html` の `sessionStorage` 仮保存を段階的に置き換える

### 6-7. 第6段階: MyBatis / DB 接続
- [〇] `ReservationMapper` を実装する
- [変] `QuestionnaireAnswerMapper` を実装する
  → 現在は `QuestionnaireResultMapper` を採用している。
- [変] `FragranceProfileMapper` を実装する
  → 現在は独立 Mapper を作らず、`questionnaire_results` 保存と予約紐付けを優先する構成へ変わっている。
- [〇] `ReservationSlotMapper` を実装する
- [ ] `StaffUserMapper` を実装する
- [〇] `reservations` テーブルを実装する
- [〇] `reservation_slots` テーブルを実装する
- [〇] `questionnaire_results` 系テーブルを実装する
- [〇] 初期データ投入方法を決める
- [〇] DB 接続確認を行う

### 6-8. 第7段階: 認証とスタッフ画面
- [〇] Spring Security の基本設定を行う
- [〇] 公開画面 `permitAll` を設定する
- [ ] スタッフログイン画面を実装する
- [ ] `StaffUserDetailsService` を実装する
- [ ] 予約一覧画面を実装する
- [ ] 予約詳細画面を実装する
- [ ] ログイン者が予約状況を確認できる導線を整える

### 6-9. 第8段階: アンケート結果処理
- [〇] `QuestionnaireApiController` を実装する
- [〇] `QuestionnaireResultService` を実装する
- [変] `FragranceProfileService` を実装する
  → 現在は独立 Service を作らず、アンケート結果保存と予約連携を優先する構成へ変わっている。
- [変] STEP1 / STEP2 分岐ロジックをサーバー側へ寄せる範囲を決める
  → 2026-03-26 時点では分岐ロジックはフロント保持のまま、graph 画面起点の API 連携を先行実装している。
- [ ] 余裕があればクエスチョンページの分岐ロジックをバックエンド連携前提へ整理する

### 6-10. 第9段階: テストと整備
- [〇] Controller テストを実装する
- [ ] Service テストを実装する
- [ ] Security テストを実装する
- [〇] 予約重複防止の確認を行う
- [〇] エラーハンドリングの確認を行う
- [ ] 主要画面の手動動作確認を行う
- [ ] テスト報告書との差分がないか確認する

### 6-11. 第10段階: README 整備
- [保] README の最終文面はユーザー管理とする
- [保] 必要に応じて実装側から記載候補のみ整理する

## 7. 本日時点で未実装
- [〇] Spring Boot アプリの起動確認
- [〇] MyBatis Mapper
- [〇] DB 接続
- [未] スタッフログイン
- [未] 予約一覧 / 予約詳細
- [〇] 自動テスト

## 8. 保留事項
- [保] GitHub リポジトリ作成と PUSH はユーザー側対応
- [保] 実運用向けメール通知や予約番号運用
- [保] 会員機能を公開導線に含めるかどうかの最終判断

## 9. 注意点
- PowerShell 上の文字化け表示だけでファイル破損と判断しない
- 共通資産とページ固有資産の責務を混ぜない
- 大きな構造変更前に、対象ファイルと変更意図を明確化する
- STEP2 や分岐処理は、資料側の分岐設計を確認してから実装する
- プロトタイプ段階の `sessionStorage` と本実装の永続保存を混同しない

## 10. 次アクション
- [〇] Spring Boot プロジェクト骨格の作成に着手する
- [〇] 既存フロント資産の `templates` / `static` 移行方針を固める
- [〇] 最初の実装単位を「画面表示成立」までに限定して進める
- [〇] Maven 実行環境を整えたうえで起動確認する
- [〇] 予約枠取得 API と予約保存 API の雛形実装へ進む
- [〇] `reservation.html` を API 呼び出し前提へ置き換える
- [ ] 永続保存前提の Model / Mapper / Service に広げる


3月16日、15：00仕様上限の為作業中断。
起動しない原因調査。
ymlファイル内の内容（DB 接続設定がない）が原因？
詳しくは３月１７日13：08以降。
再開時はマークダウン内容及び前日チャット内容を下記に記述するので確認
なお、java21はインスコされてるはず

-mvnってまだ、コマンド見つかりませんか？


1 件の過去のメッセージ
はい。現時点でも mvn は見つかっていません。

確認結果は以下です。

Get-Command mvn → 見つからない
mvn -v → コマンド未認識
where.exe mvn → 該当なし
つまり、この環境の PATH 上には Maven がまだ入っていません。Maven をインストールして PATH を通すか、あるいは Maven Wrapper を用意しない限り、こちらから mvn spring-boot:run や mvn test は実行できない状態です。


ユーザーの添付画像
取得してきますので、添付画像でどれを選択するのかか、及び選択してDLしたファイルをどこに置き換えるのかのレクチャーをお願いします

選ぶ内容はこれです。

Project: Maven
Language: Java
Spring Boot: 3.5.11
Group: com.fregrance
Artifact: fregrance
Name: fregrance
Description: Fragrance workshop portfolio application
Package name: com.fregrance
Packaging: Jar
Configuration: YAML
Java: 21
ADD DEPENDENCIES... では次を追加してください。

Spring Web
Thymeleaf
Validation
MyBatis Framework
Spring Security
MySQL Driver
Lombok
Spring Boot DevTools
ダウンロードした後の置き換え方です。

zip を展開する
展開先の中身を確認する
主に pom.xml, src, .mvn, mvnw, mvnw.cmd, HELP.md
今の作業フォルダ C:\academia\src\Fregrance に対して、次の扱いにする
pom.xml はダウンロード版で上書きしてよい
.mvn, mvnw, mvnw.cmd は今のルートに追加
src は丸ごと上書きしない
src については、展開した側の src から必要なのは最小限です
src/main/java/com/fregrance/FregranceApplication.java は使わず、今ある FregranceApplication.java を維持
src/main/resources/application.yml は内容を見比べて必要なら統合
それ以外の既存 src/main/resources/templates, static, controller, service などは今の作業内容を優先
つまり実務上は、
pom.xml
.mvn/
mvnw
mvnw.cmd
だけを今のルートに持ってくるのが安全です
補足です。

ダウンロードした src をそのまま今の src に上書きすると、こちらで作成済みの controller, service, template が壊れます
まずは Wrapper を入れて .\mvnw.cmd -v が通る状態にするのが目的です
配置先の最終形はこうです。

pom.xml
C:\academia\src\Fregrance\.mvn\
C:\academia\src\Fregrance\mvnw
C:\academia\src\Fregrance\mvnw.cmd
そこまでできたら、こちらで次に mvnw ベースで起動確認に進めます。


C:\academia\src\Fregrance\企画案md\仮fregranceの中に展開したファイル群を入れてあります。必要なものはここから置き換えてください。作業狩猟後、このフォルダは消していただいて構いません。


4 件の過去のメッセージ
必要なものは取り込みました。

反映したのは次です。

pom.xml
C:\academia\src\Fregrance\.mvn\
C:\academia\src\Fregrance\mvnw
C:\academia\src\Fregrance\mvnw.cmd
src は既存実装を守るため取り込んでいません。展開用フォルダ 企画案md/仮fregrance は削除済みです。

現時点で mvnw.cmd は実行対象として認識されています。次はこれで .\mvnw.cmd -v を実行して、Wrapper 経由で Maven が使えるか確認できます。


-それでは、お願いします


3 件の過去のメッセージ
通りました。mvnw.cmd 経由で Maven は使える状態です。

確認結果:

Maven: 3.9.12
Java: 17.0.17
Maven home: C:\Users\academia\.m2\wrapper\dists\...
つまり、Maven Wrapper 自体は正常に動作しています。先ほど失敗していたのは mvnw.cmd の PowerShell 判定部分で、そこを最小修正して解消しました。mvnw.cmd

ただし、現時点の Java はまだ 17 です。方針書と Initializr の想定は Java 21 なので、

今すぐ pom.xml を 17 に合わせる
先に PC 側の Java 21 を入れてそのまま進める
のどちらかが必要です。
実装方針どおりに進めるなら、次は Java 21 の確認をしてから .\mvnw.cmd test か .\mvnw.cmd spring-boot:run に進むのが正しいです。


『先に PC 側の Java 21 を入れてそのまま進める
のどちらかが必要です。』とのことですが、こちらでする作業はありますか？

あります。方針どおり Java 21 で進めるなら、こちらで必要なのは「PC に Java 21 を入れて、ターミナルから 21 が見える状態にする」ことです。

やることはこれだけです。

Java 21 をインストールする
JAVA_HOME を Java 21 に向ける
Path の先頭側に Java 21 の bin を入れる
新しいターミナルを開く
java -version で 21 になっていることを確認する
確認結果がこうなれば完了です。

java -version
出力の先頭が 21 系ならOKです。

補足です。

いまは mvnw.cmd 自体は動いています
ただし Maven は現在見えている Java を使うので、Java 17 のままだと pom.xml の java.version=21 と食い違います
そのため、こちらが続きの .\mvnw.cmd test や .\mvnw.cmd spring-boot:run を安全に進めるには、まず Java 21 が必要です
Java 21 のインストール後に、こちらで java -version を再確認して続行できます。


-これでどうでしょうか？


1 件の過去のメッセージ
まだ切り替わっていません。こちらから見えているのは引き続き Java 17 です。

確認結果:

java -version → 17.0.17
javac -version → 17.0.17
JAVA_HOME → 未設定
つまり、Java 21 のインストール自体は済んでいても、

JAVA_HOME が未設定
Path がまだ Java 17 を見ている
あるいはターミナルを開き直していない
のどれかです。
次にやること:

Java 21 のインストール先を確認
例: C:\Program Files\Eclipse Adoptium\jdk-21...
JAVA_HOME をそのフォルダに設定
Path に %JAVA_HOME%\bin を追加し、Java 17 の bin より前に置く
ターミナルを閉じて開き直す
その後、もう一度こちらで確認します。


-これでどうでしょうか？



3 件の過去のメッセージ
まだ切り替わっていません。こちらから見えているのは引き続き Java 17 です。

確認結果:

java -version -> 17.0.17
javac -version -> 17.0.17
JAVA_HOME -> 未設定
.\mvnw.cmd -v -> Java version: 17.0.17
つまり、現状は Maven Wrapper 自体は正常ですが、使っている JDK はまだ C:\Program Files\Eclipse Adoptium\jdk-17.0.17.10-hotspot です。Java 21 を入れていても、JAVA_HOME と Path がまだ 17 側のままです。

次に確認すべき点はこれです。

Java 21 の実際のインストール先を確認する
JAVA_HOME をその JDK 21 フォルダに設定する
Path の先頭側に %JAVA_HOME%\bin を置く
IDE かターミナルを完全に開き直す
再確認できれば十分なので、設定後にもう一度 これでどうでしょうか？ と送ってください。


-Codex メッセージの使用上限に達しました
-今すぐアクセス回数を増やすには、管理者にリクエストを送信するか、2026/03/17 13:08までお待ちください
-使用状況を確認する
