# 2026-03-19 MySQL導入とDocker対応 作業要約

## 1. 本日の目的
- Render でのビルド・公開を見据えて、Spring Boot + Maven プロジェクトとして不足している前提を埋める。
- 特に MySQL 導入、ローカル起動確認、テスト成功、Docker ビルド確認までを進める。

## 2. 本日実施したこと

### 2-1. 既存構成と状況確認
- リポジトリ構成、`git status`、主要ソース、直近の作業 Markdown を確認した。
- `src/main` 側は Spring Boot + Thymeleaf + MyBatis + MySQL Connector 前提の構成になっていることを確認した。
- `/` はすでにトップページへ到達する構成であり、`/todo` へのリダイレクトは不要であることを確認した。

### 2-2. 文字化け関連の確認
- 作業 Markdown を確認し、文字化けには次の2系統があることを再確認した。
  - PowerShell 表示上だけの文字化け
  - Shell 経由の書き戻しによる実ファイル破損
- 今後も、日本語ファイルの確認時は表示問題と実体問題を分けて扱う前提を再確認した。

### 2-3. MySQL の導入
- Windows 上に MySQL Community Server 8.0.45 をインストールした。
- `MySQL80` サービスが起動していることを確認した。
- `root` ログイン確認を行い、MySQL が利用可能であることを確認した。

### 2-4. DB とユーザーの作成
- 以下を実施した。
  - `fregrance` データベース作成
  - `fregrance_user` ユーザー作成
  - `fregrance_user@localhost` に `fregrance.*` への権限付与
- 現在のローカル確認用パスワードは `proclimb`
  - これはポートフォリオ確認用の暫定値であり、実運用前提では変更が必要

### 2-5. Spring Boot 側設定の修正
- `src/main/resources/application.yml` を修正した。
- 追加・変更内容:
  - `spring.datasource.*` を追加
  - ローカル既定値は `fregrance_user / proclimb`
  - `SPRING_DATASOURCE_URL` / `SPRING_DATASOURCE_USERNAME` / `SPRING_DATASOURCE_PASSWORD` で上書き可能にした
  - `server.port` を `${PORT:8080}` に変更し、Render 側の `PORT` を受けられるようにした

### 2-6. ローカル起動確認
- `./mvnw.cmd spring-boot:run` 実行後に以下を確認した。
  - `http://localhost:8080/api/health` が `{"status":"ok"}` を返す
  - `http://localhost:8080/` が問題なく表示される

### 2-7. テスト確認
- `./mvnw.cmd test` を実行し、`BUILD SUCCESS` を確認した。
- 結果:
  - Tests run: 3
  - Failures: 0
  - Errors: 0
  - Skipped: 0

### 2-8. Docker 対応
- `Dockerfile` を新規作成した。
  - Java 21 のマルチステージビルド
  - Maven Wrapper を利用して jar を作成
  - 実行イメージは JRE ベース
- `.dockerignore` を新規作成した。
  - `target`、`.git`、`.vscode`、資料フォルダなどを除外対象に追加した
- `docker build -t fregrance-app .` を実行し、ビルド成功を確認した

## 3. 現在の状況
- Spring Boot アプリはローカルで起動可能
- MySQL 接続込みで動作可能
- `mvn test` 成功
- `docker build` 成功
- Render 公開に向けた最低限の前提はかなり整った状態

## 4. 現時点で整っている前提
- Java プロジェクト（Spring Boot + Maven）がローカルに存在している
- Maven Wrapper がある
- MySQL がローカルに導入されている
- DB 接続設定がある
- `PORT` 環境変数に対応している
- Dockerfile / `.dockerignore` がある

## 5. まだ残っていること
- Render で利用する DB 接続先を最終決定すること
- Render 側で設定する環境変数を本番値にすること
  - `SPRING_DATASOURCE_URL`
  - `SPRING_DATASOURCE_USERNAME`
  - `SPRING_DATASOURCE_PASSWORD`
- 必要なら `docker run` でのコンテナ起動確認を追加で行うこと
- ポートフォリオ段階を超えて公開品質に上げるなら、暫定パスワードを変更すること

## 6. 重要な補足
- `/` はすでにトップページへ到達できるため、このプロジェクトで `/todo` リダイレクト修正は不要
- 文字化け関連は、PowerShell 表示だけで破損と判断しないこと
- 日本語を含むファイルは、今後も最小差分編集を基本にすること

## 7. 本日変更した主なファイル
- `src/main/resources/application.yml`
- `Dockerfile`
- `.dockerignore`
