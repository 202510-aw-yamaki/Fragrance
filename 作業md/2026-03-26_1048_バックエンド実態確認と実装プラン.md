# 2026-03-26 10:48 バックエンド実態確認と実装プラン

## 1. 目的
- 2026-03-19 時点の記録と、2026-03-26 時点の現コード・現環境を照合し、実際に動いているものと未実装・未検証のものを切り分ける。
- ルート直下フロントと `src/main/resources` 側のズレを明示し、次の実装で前提破綻や重複対応を起こさないようにする。
- このまま着手可能な現実的バックエンド実装プランを、チェックリストとして残す。

## 2. 確認対象
- ルート直下フロント: `index.html` `questionnaire.html` `questionnaire_step2.html` `fragrance-graph.html` `reservation.html` `reservation-complete.html` `css/` `img/`
- Spring Boot 側: `src/main/java` `src/main/resources/templates` `src/main/resources/static` `src/main/resources/*.sql`
- テスト: `src/test/java`
- 関連記録: `作業md/3-19まで/*.md`

## 3. 本日時点の事実ベース整理
### 3-1. 3/19 時点の前提で、今も有効なもの
- [x] `src/main` 側は DB 前提で進める方針
- [x] MySQL 接続設定は `src/main/resources/application.yml` に存在する
- [x] `server.port=${PORT:8080}` 対応済み
- [x] `Dockerfile` と `.dockerignore` は存在する
- [x] ルート直下の静的フロントは維持対象、実装本線は `src/main/...`

### 3-2. 3/17 時点の古い前提で、今は採用しないもの
- [ ] `DB 未設定でも起動する暫定対応` を本線前提として再採用しない
- [ ] `MySQL 未導入で停止中` という認識を持ち込まない

## 4. 実際に動いているもの
### 4-1. 起動・テスト
- [x] `./mvnw.cmd test` は 2026-03-26 に実行し、`BUILD SUCCESS` を確認
- [x] テスト件数は 3 件、失敗 0、エラー 0
- [x] テストログ上で Java 21 と MySQL 接続の成立を確認

### 4-2. 画面返却
- [x] `FragrancePageController` で以下の画面ルートが存在する
- [x] `/` `/index.html`
- [x] `/questionnaire` `/questionnaire.html`
- [x] `/questionnaire/step2` `/questionnaire_step2.html`
- [x] `/graph` `/fragrance-graph.html`
- [x] `/reservation` `/reservation.html`
- [x] `/reservation/complete` `/reservation-complete.html`

### 4-3. API
- [x] `/api/health` はテストで 200 応答を確認
- [x] `/api/reservation-slots?date=...` はテストで 200 応答を確認
- [x] `/api/reservations` はテストで 201 応答を確認
- [ ] `/api/questionnaire-results` は実装済みだが、本日時点で疎通テスト未確認

## 5. 実際には動いていない、または実装が半端なもの
### 5-1. 予約枠 API の実態
- [x] `ReservationSlotService` は DB から予約枠を読みに行く実装を持つ
- [x] ただし DB で `reservation_slots` が空の場合はモック3件へフォールバックする
- [ ] `data.sql` に予約枠初期データは入っていない
- [ ] したがって、現時点の予約枠 API は「DB運用」ではなく「モック返却が成立している」状態の可能性が高い

### 5-2. 予約登録 API の実態
- [x] `ReservationService` は DB 保存ルートを持つ
- [x] ただし `slotId` に対応する `reservation_slots` が DB に存在しない場合、DB insert せずにレスポンスだけ返す実装になっている
- [x] `visit_types` は `data.sql` に存在するが、`reservation_slots` 初期データがないため DB 永続化が成立しない可能性が高い
- [ ] 現時点で `POST /api/reservations` の 201 は確認済みだが、`予約がDBへ確実に保存されている` ことは未保証
- [ ] 重複予約防止、枠の確保状態更新、トランザクション制御は未実装

### 5-3. アンケート結果 API の実態
- [x] `QuestionnaireApiController` `QuestionnaireResultService` `QuestionnaireResultMapper` は存在する
- [x] DB 保存ルートとメモリ保持ルートの両方がある
- [ ] フロント側の `questionnaire.html` `questionnaire_step2.html` `fragrance-graph.html` はまだ `sessionStorage` 中心
- [ ] アンケート API はフロント未接続
- [ ] API の疎通テストがない

### 5-4. 認証・管理画面
- [x] `SecurityConfig` はある
- [x] 公開ページと公開 API は `permitAll`
- [ ] スタッフ用ユーザー定義、ログイン画面、予約一覧、予約詳細は未実装
- [ ] 現在の認証は Spring Security のデフォルトユーザー生成に依存している

## 6. フロント実態と同期ズレ
### 6-1. ルート直下 HTML と `src/main/resources/templates` の比較
- [x] `questionnaire.html` は一致
- [x] `questionnaire_step2.html` は一致
- [x] `fragrance-graph.html` は一致
- [x] `reservation.html` は一致
- [△] `reservation-complete.html` は差分があるが、実質は整形差分が中心
- [ ] `index.html` は不一致

### 6-2. `index.html` の不一致内容
- [ ] ルート直下 `index.html` にある Basic Information セクションが、`src/main/resources/templates/index.html` には未反映
- [ ] ルート直下 `index.html` にある運営情報・連絡先付きフッターが、`src/main/resources/templates/index.html` には未反映
- [ ] つまり、Spring Boot 側トップページは 3/24-3/25 のフロント最終状態に追従できていない

### 6-3. CSS の不一致内容
- [x] `common.css` `fragrance-graph.css` `questionnaire-common.css` `questionnaire-step1.css` `questionnaire-step2.css` `reservation.css` `reservation-complete.css` はルートと `src/main/resources/static/css` 側で実質同期している
- [ ] `top-page.css` は不一致
- [ ] ルート直下 `css/top-page.css` にある Basic Information / フッター関連スタイルが、`src/main/resources/static/css/top-page.css` に未反映

### 6-4. 画像の不一致内容
- [x] `questionnaire/` と `questionnaire2/` の画像はルートと `src/main/resources/static/img` で一致
- [x] TOP 画像の一部は一致
- [ ] 以下の画像はルート直下 `img/TOP` にあるが、`src/main/resources/static/img/TOP` に存在しない
- [ ] `roherarogo.png`
- [ ] `吟ロゴ.png`
- [ ] `グーグルマップ地図上野教室.png`
- [ ] `看板.png`
- [ ] `香水ハンドクリーム.png`
- [ ] `値段表.png`
- [ ] `上野駅.png`
- [ ] `料金表の上の画像.png`
- [ ] `ヘッダーロゴ.png`
- [ ] `香水ハンドクリーム.png`
- [ ] `調整.png`
- [ ] そのため、Spring Boot 側テンプレートで参照しているロゴ画像と QR 画像は表示欠落の可能性が高い

## 7. 画面ごとの現実的な状態
### 7-1. questionnaire.html
- [x] 画面自体は存在
- [x] `sessionStorage` に STEP1 回答を保存
- [ ] バックエンド未接続

### 7-2. questionnaire_step2.html
- [x] 画面自体は存在
- [x] STEP1 の `sessionStorage` を読んで分岐
- [ ] バックエンド未接続

### 7-3. fragrance-graph.html
- [x] 画面自体は存在
- [x] グラフ調整値を `sessionStorage` へ保存
- [ ] バックエンド未接続

### 7-4. reservation.html
- [x] 画面自体は存在
- [x] 予約枠取得 API を呼ぶ実装あり
- [x] 予約登録 API を呼ぶ実装あり
- [x] API 失敗時の表示あり
- [ ] 予約枠 DB 実データ化は未完
- [ ] 予約登録の DB 永続化保証は未完

### 7-5. reservation-complete.html
- [x] 画面自体は存在
- [x] `sessionStorage` から予約内容を読みつつ、`reservationCode` があれば `/api/reservations/{code}` 取得を試す実装あり
- [ ] QR 画像などの静的資産不足により、Spring Boot 側では見た目崩れの可能性あり

### 7-6. index.html
- [ ] ルート直下版が最新
- [ ] Spring Boot テンプレート版は古い
- [ ] 画像不足もあり、Spring Boot 側トップページはフロント基準ファイルと一致していない

## 8. 現時点の結論
- [x] バックエンドは「完全未着手」ではない
- [x] `health` と `reservation` の最低限 API は実動確認済み
- [x] MySQL 接続込みでテストは通る
- [ ] ただし予約機能は「API が返る」段階であり、「DB を正として予約運用できる」段階ではない
- [ ] アンケート結果 API は土台止まり
- [ ] Spring Boot 側のトップページと画像群は、ルート直下フロント基準とズレている

## 9. 次の実装プラン
### 9-1. P0 実態合わせ
- [ ] ルート直下 `index.html` を `src/main/resources/templates/index.html` へ同期する
- [ ] ルート直下 `css/top-page.css` を `src/main/resources/static/css/top-page.css` へ同期する
- [ ] ルート直下 `img/TOP` の不足画像を `src/main/resources/static/img/TOP` へ補充する
- [ ] `reservation-complete.html` が参照する QR 画像とロゴ画像の存在を解消する
- [ ] Spring Boot 側トップページが、クライアント確認済みフロント基準と一致する状態にする

### 9-2. P1 予約機能を「返る」から「保存できる」へ上げる
- [ ] `reservation_slots` 初期データ投入方針を決める
- [ ] `data.sql` または別初期化手段で予約枠データを投入する
- [ ] `GET /api/reservation-slots` が DB 実データ返却になっていることを確認する
- [ ] `POST /api/reservations` 実行後に DB レコードが作成されることを確認する
- [ ] 予約完了画面の `reservationCode` 再取得が DB 実データで成立することを確認する

### 9-3. P1 予約ロジック不足の解消
- [ ] 予約登録時に対象枠の存在チェックをエラー応答へ明示する
- [ ] 枠未存在時に `201` を返す現在仕様を見直す
- [ ] 同一枠の重複予約防止を入れる
- [ ] 必要なら `reservation_slots.status` の更新ルールを定義する
- [ ] トランザクション境界を予約作成処理へ入れる

### 9-4. P2 アンケート連携
- [ ] `POST /api/questionnaire-results` と `GET /api/questionnaire-results/{code}` の疎通確認を追加する
- [ ] まずは graph 画面から questionnaire-results 保存 API を呼ぶ方針にする
- [ ] `reservation.html` で `sessionStorage` 依存の読み込みを、API 取得へ段階移行する
- [ ] `reservation-complete.html` へアンケート結果要約を引き渡す設計を固める

### 9-5. P3 テスト拡充
- [ ] Questionnaire API の統合テストを追加する
- [ ] 予約登録の DB 永続化確認テストを追加する
- [ ] 予約枠が空のときモック返却になる現挙動を、残すか廃止するか決める
- [ ] 重複予約防止とバリデーション失敗のテストを追加する

## 10. 着手順の推奨
1. [ ] Spring Boot 側トップページと静的資産の同期
2. [ ] 予約枠データ投入
3. [ ] 予約登録の DB 永続化確認
4. [ ] 予約登録失敗時のエラー仕様整理
5. [ ] Questionnaire API の疎通確認とフロント接続開始
6. [ ] テスト拡充

## 11. 次回作業の開始条件
- [ ] 上記 P0 が完了し、Spring Boot 側画面がルート直下フロント基準と一致している
- [ ] `reservation_slots` に実データが入り、モック依存なしで枠一覧が返る
- [ ] `POST /api/reservations` の結果が DB に残ることを確認できている
- [ ] その状態になってから、アンケート API 連携へ進む