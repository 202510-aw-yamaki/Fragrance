# 2026-03-26 10:48 バックエンド実態確認と実装プラン

## 1. 目的
- 2026-03-19 時点の記録と、2026-03-26 時点の現コード・現環境を照合し、実際に動いているものと未実装・未検証のものを切り分ける。
- ルート直下フロントと `src/main/resources` 側のズレを明示し、次の実装で前提破綻や重複対応を起こさないようにする。
- このまま着手可能な現実的バックエンド実装プランを、チェックリストとして残す。

## 2. 確認対象
- ルート直下フロント: `index.html` `questionnaire.html` `questionnaire_step2.html` `fragrance-graph.html` `reservation.html` `reservation-complete.html` `css/` `img/`
- Spring Boot 側: `src/main/java` `src/main/resources/templates` `src/main/resources/static` `src/main/resources/*.sql`
- テスト: `src/test/java`
- 関連記録: `作業md/3-19まで/*.md`

## 3. 本日時点の事実ベース整理
### 3-1. 3/19 時点の前提で、今も有効なもの
- [x] `src/main` 側は DB 前提で進める方針
- [x] MySQL 接続設定は `src/main/resources/application.yml` に存在する
- [x] `server.port=${PORT:8080}` 対応済み
- [x] `Dockerfile` と `.dockerignore` は存在する
- [x] ルート直下の静的フロントは維持対象、実装本線は `src/main/...`

### 3-2. 3/17 時点の古い前提で、今は採用しないもの
- [ ] `DB 未設定でも起動する暫定対応` を本線前提として再採用しない
- [ ] `MySQL 未導入で停止中` という認識を持ち込まない

## 4. 実際に動いているもの
### 4-1. 起動・テスト
- [x] `./mvnw.cmd test` は 2026-03-26 に実行し、`BUILD SUCCESS` を確認
- [x] テスト件数は 12 件、失敗 0、エラー 0
- [x] テストログ上で Java 21 と MySQL 接続の成立を確認

### 4-2. 画面返却
- [x] `FragrancePageController` で以下の画面ルートが存在する
- [x] `/` `/index.html`
- [x] `/questionnaire` `/questionnaire.html`
- [x] `/questionnaire/step2` `/questionnaire_step2.html`
- [x] `/graph` `/fragrance-graph.html`
- [x] `/reservation` `/reservation.html`
- [x] `/reservation/complete` `/reservation-complete.html`

### 4-3. API
- [x] `/api/health` はテストで 200 応答を確認
- [x] `/api/reservation-slots?date=...` はテストで 200 応答を確認
- [x] `/api/reservations` はテストで 201 応答を確認
- [x] `/api/questionnaire-results` は POST / GET の疎通テスト確認済み

## 5. 実際には動いていない、または実装が半端なもの
### 5-1. 予約枠 API の実態
- [x] `ReservationSlotService` は DB から予約枠を読みに行く実装を持つ
- [x] DB で `reservation_slots` が空の場合は空配列を返す実装へ見直し済み
- [x] `data.sql` に今後 14 日分 x 3 枠の予約枠初期データ投入を追加した
- [x] `ReservationApiTests` で DB 専用スロットを挿入し、予約枠 API が DB 実データを返すことを確認した

### 5-2. 予約登録 API の実態
- [x] `ReservationService` は DB 保存ルートを持つ
- [x] `slotId` に対応する `reservation_slots` が DB に存在しない場合は 404 を返す実装へ見直し済み
- [x] `visit_types` と `reservation_slots` の初期データは `data.sql` で投入される状態に更新した
- [x] `ReservationApiTests` で DB 実在スロットに対する予約登録後、`reservations` テーブルへ保存されることを確認した
- [x] 重複予約防止、枠の確保状態更新、トランザクション制御を実装した

### 5-3. アンケート結果 API の実態
- [x] `QuestionnaireApiController` `QuestionnaireResultService` `QuestionnaireResultMapper` は存在する
- [x] DB 保存ルートとメモリ保持ルートの両方がある
- [△] フロント側の `questionnaire.html` `questionnaire_step2.html` `fragrance-graph.html` は `sessionStorage` を併用しつつ、graph 以降は API 連携開始済み
- [x] アンケート API は graph / reservation 導線でフロント接続済み
- [x] API の疎通テストは QuestionnaireApiTests で追加済み

### 5-4. 認証・管理画面
- [x] `SecurityConfig` はある
- [x] 公開ページと公開 API は `permitAll`
- [ ] スタッフ用ユーザー定義、ログイン画面、予約一覧、予約詳細は未実装
- [ ] 現在の認証は Spring Security のデフォルトユーザー生成に依存している

## 6. フロント実態と同期ズレ
### 6-1. ルート直下 HTML と `src/main/resources/templates` の比較
- [x] `questionnaire.html` は一致
- [x] `questionnaire_step2.html` は一致
- [x] `fragrance-graph.html` は一致
- [x] `reservation.html` は一致
- [△] `reservation-complete.html` は差分があるが、実質は整形差分が中心
- [x] `index.html` は同期済み

### 6-2. `index.html` の不一致内容
- [x] ルート直下 `index.html` にある Basic Information セクションは、`src/main/resources/templates/index.html` へ反映済み
- [x] ルート直下 `index.html` にある運営情報・連絡先付きフッターは、`src/main/resources/templates/index.html` へ反映済み
- [x] つまり、Spring Boot 側トップページは 3/24-3/25 のフロント最終状態へ追従済み

### 6-3. CSS の不一致内容
- [x] `common.css` `fragrance-graph.css` `questionnaire-common.css` `questionnaire-step1.css` `questionnaire-step2.css` `reservation.css` `reservation-complete.css` はルートと `src/main/resources/static/css` 側で実質同期している
- [x] `top-page.css` は同期済み
- [x] ルート直下 `css/top-page.css` にある Basic Information / フッター関連スタイルは、`src/main/resources/static/css/top-page.css` へ反映済み

### 6-4. 画像の不一致内容
- [x] `questionnaire/` と `questionnaire2/` の画像はルートと `src/main/resources/static/img` で一致
- [x] TOP 画像は必要分を Spring Boot 側へ補充済み
- [x] 以前不足していたルート直下 `img/TOP` の画像は、`src/main/resources/static/img/TOP` へ補充済み
- [x] `roherarogo.png`
- [x] `吟ロゴ.png`
- [x] `グーグルマップ地図上野教室.png`
- [x] `看板.png`
- [x] `香水ハンドクリーム.png`
- [x] `値段表.png`
- [x] `上野駅.png`
- [x] `料金表の上の画像.png`
- [x] `ヘッダーロゴ.png`
- [x] `香水ハンドクリーム.png`
- [x] `調整.png`
- [x] そのため、Spring Boot 側テンプレートで参照しているロゴ画像と QR 画像は表示欠落の可能性は解消済み

## 7. 画面ごとの現実的な状態
### 7-1. questionnaire.html
- [x] 画面自体は存在
- [x] `sessionStorage` に STEP1 回答を保存
- [ ] バックエンド未接続（保存 API 呼び出しは graph 画面起点）

### 7-2. questionnaire_step2.html
- [x] 画面自体は存在
- [x] STEP1 の `sessionStorage` を読んで分岐
- [△] `routeCode` と STEP2 回答保持までは実装済み、保存 API 呼び出しは graph 画面起点

### 7-3. fragrance-graph.html
- [x] 画面自体は存在
- [x] グラフ調整値を `sessionStorage` へ保存
- [x] `questionnaire-results` 保存 API と接続済み

### 7-4. reservation.html
- [x] 画面自体は存在
- [x] 予約枠取得 API を呼ぶ実装あり
- [x] 予約登録 API を呼ぶ実装あり
- [x] API 失敗時の表示あり
- [x] 予約枠 DB 実データ化は完了
- [x] 予約登録の DB 永続化保証は確認済み

### 7-5. reservation-complete.html
- [x] 画面自体は存在
- [x] `sessionStorage` から予約内容を読みつつ、`reservationCode` があれば `/api/reservations/{code}` 取得を試す実装あり
- [x] P0 対応で QR 画像・ロゴ画像の存在不足は解消した

### 7-6. index.html
- [x] ルート直下版を Spring Boot 側へ同期済み
- [x] Spring Boot テンプレート版の旧状態は解消した
- [x] P0 対応でトップページ本体と必要画像の不一致は解消した

## 8. 現時点の結論
- [x] バックエンドは「完全未着手」ではない
- [x] `health` と `reservation` の最低限 API は実動確認済み
- [x] MySQL 接続込みでテストは通る
- [x] 予約機能は DB を正として枠取得・予約登録・完了画面再取得まで確認済み
- [x] アンケート結果 API はフロント接続と DB 疎通確認まで完了
- [x] P0 対応で Spring Boot 側トップページと必要画像群のズレは解消した

## 9. 次の実装プラン
### 9-1. P0 実態合わせ
- [x] ルート直下 `index.html` を `src/main/resources/templates/index.html` へ同期する
- [x] ルート直下 `css/top-page.css` を `src/main/resources/static/css/top-page.css` へ同期する
- [x] ルート直下 `img/TOP` の不足画像を `src/main/resources/static/img/TOP` へ補充する
- [x] `reservation-complete.html` が参照する QR 画像とロゴ画像の存在を解消する
- [x] Spring Boot 側トップページが、クライアント確認済みフロント基準と一致する状態にする

### 9-2. P1 予約機能を「返る」から「保存できる」へ上げる
- [x] `reservation_slots` 初期データ投入方針を決める
- [x] `data.sql` または別初期化手段で予約枠データを投入する
- [x] `GET /api/reservation-slots` が DB 実データ返却になっていることを確認する
- [x] `POST /api/reservations` 実行後に DB レコードが作成されることを確認する
- [x] 予約完了画面の `reservationCode` 再取得が DB 実データで成立することを確認する

### 9-3. P1 予約ロジック不足の解消
- [x] 予約登録時に対象枠の存在チェックをエラー応答へ明示する
- [x] 枠未存在時に `201` を返す現在仕様を見直す
- [x] 同一枠の重複予約防止を入れる
- [x] `reservation_slots.status` は予約確定時に `reserved` へ更新するルールを定義した
- [x] トランザクション境界を予約作成処理へ入れる

### 9-4. P2 アンケート連携
- [x] `POST /api/questionnaire-results` と `GET /api/questionnaire-results/{code}` の疎通確認を追加する
- [x] まずは graph 画面から questionnaire-results 保存 API を呼ぶ方針にする
- [x] `reservation.html` で `sessionStorage` 依存の読み込みを、API 取得へ段階移行する
- [※] `reservation-complete.html` へアンケート結果要約を引き渡す設計を固める 
　※ 現段階の要請は閲覧者に違和感のない画面とバックエンド機能成立であるため、完了画面への要約表示は見送り、内部的なアンケート結果保存と予約レコードとの紐付けまでを実装済みとした。

### 9-5. P3 テスト拡充
- [x] Questionnaire API の統合テストを追加する
- [x] 予約登録の DB 永続化確認テストを追加する
- [x] 予約枠が空のときモック返却になる現挙動を、残すか廃止するか決める
→ [x] ユーザー合意のもと、予約枠は日時1つにつき1件で受付終了とし、空日のモック返却を廃止して空配列返却へ統一したことにより完了扱い
- [x] 重複予約防止とバリデーション失敗のテストを追加する

## 10. 着手順の推奨
1. [x] Spring Boot 側トップページと静的資産の同期
2. [x] 予約枠データ投入
3. [x] 予約登録の DB 永続化確認
4. [x] 予約登録失敗時のエラー仕様整理
5. [x] Questionnaire API の疎通確認とフロント接続開始
6. [x] テスト拡充

## 11. 次回作業の開始条件
- [x] 上記 P0 が完了し、Spring Boot 側画面がルート直下フロント基準と一致している
- [x] `reservation_slots` に実データが入り、モック依存なしで枠一覧が返る
- [x] `POST /api/reservations` の結果が DB に残ることを確認できている
- [x] その状態でアンケート API 連携へ進み、9-4 まで完了した
## 12. 2026-03-26 P0 対応結果
- [x] index.html と src/main/resources/templates/index.html のハッシュ一致を確認
- [x] css/top-page.css と src/main/resources/static/css/top-page.css のハッシュ一致を確認
- [x] ルート直下 img/TOP に存在する画像が、src/main/resources/static/img/TOP に全件存在することを確認
- [ ] reservation-complete.html と src/main/resources/templates/reservation-complete.html の完全一致は未対応
- [x] ただし今回の P0 で必要だった QR 画像・ロゴ画像の存在は解消済み
- [ ] 残差分は reservation-complete.html の整形差分であり、次段階で要否判断する
## 13. 2026-03-26 P1 予約枠データ投入結果
- [x] data.sql に、CURDATE() 基準で今後 14 日分 x 3 枠の予約枠投入 SQL を追加した
- [x] MySQL 直接確認で reservation_slots は 42 件、期間は 2026-03-27 から 2026-04-09 であることを確認した
- [x] ReservationApiTests を DB 実データ確認型へ更新した
- [x] 予約枠 API は、DB にだけ存在する 2099-12-31 18:00 / DbVerifier を返すことをテストで確認した
- [x] 予約登録 API は、DB に存在するスロットに対して reservations テーブルへ永続化されることをテストで確認した
- [x] reservationCode を使った完了画面の再取得確認は実施済み
- [x] 予約枠未存在時の 201 応答見直し、重複予約防止、枠状態更新は 9-3 で対応済み
## 14. 2026-03-26 完了画面再取得確認結果
- [x] ReservationApiTests に getReservationReturnsPersistedReservationForCompletePageReload を追加した
- [x] DB 実在スロットに対して予約を作成し、返却された reservationCode で GET /api/reservations/{reservationCode} を実行した
- [x] 応答は 200 OK で、slotLabel visitType guestCount を含むことを確認した
- [x] reservation-complete.html が依存する再取得前提は、API レベルで成立していることを確認した
- [x] ブラウザ実画面で sessionStorage なし・クエリパラメータありの完了画面手動確認は実施済み
## 15. 2026-03-26 9-3 対応結果
- [x] ReservationService をトランザクション化した
- [x] 予約対象枠が存在しない場合は 404 NOT_FOUND を返すよう変更した
- [x] 予約対象枠が既に確保済みの場合は 409 RESERVATION_CONFLICT を返すよう変更した
- [x] 予約確定時に reservation_slots.status を reserved へ更新する処理を追加した
- [x] ApiExceptionHandler に業務例外ハンドラを追加し、JSON エラー応答を統一した
- [x] ReservationApiTests に 404 / 409 ケースを追加し、mvn test 成功を確認した
## 16. 2026-03-26 9-4 対応結果
- [x] QuestionnaireApiTests を追加し、POST /api/questionnaire-results と GET /api/questionnaire-results/{code} の DB 疎通を確認した
- [x] questionnaire_step2.html で STEP2 回答と routeCode を sessionStorage へ保持するよう更新した
- [x] fragrance-graph.html から questionnaire-results 保存 API を呼び、resultCode を reservation.html?resultCode=... へ引き渡すよう更新した
- [x] reservation.html は resultCode がある場合にアンケート結果 API を再取得し、取得失敗時のみ既存 sessionStorage フォールバックを使う構成へ変更した
- [x] ./mvnw.cmd test で既存予約 API を含めたテスト一式が成功した

## 17. 2026-03-26 予約とアンケート結果の内部紐付け
- [x] 完了画面へのアンケート要約表示は見送り、画面表示要件は維持した
- [x] reservations.questionnaire_result_code を追加し、予約時に questionnaireResultCode を保存するよう変更した
- [x] reservation.html は保持中の resultCode を予約登録 API へ渡すよう更新した
- [x] ReservationApiTests に、予約へアンケート結果コードが保存されることを確認する統合テストを追加した
- [x] 既存 DB にはローカル MySQL へ列追加を適用し、./mvnw.cmd clean test の成功を確認した


## 18. 2026-03-26 9-5 対応結果
- [x] ReservationSlotService の空枠時モック返却を廃止し、空配列返却へ変更した
- [x] ReservationApiTests に、予約枠が存在しない日付で空配列が返ることを確認する統合テストを追加した
- [x] ReservationApiTests に、必須項目欠落時と staffMemo 文字数超過時のバリデーション失敗テストを追加した
- [x] .\\mvnw.cmd test を再実行し、テスト総数 12 件で成功を確認した
