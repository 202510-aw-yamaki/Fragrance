# 2026-03-25 11:38 ヘッダー調整と基本情報エリア調整まとめ

## 対象
- トップページヘッダー
- 基本情報 (`#information`) エリア
- 静的HTML側と Spring Boot 側テンプレート / CSS の同期

## 対象ファイル
- `index.html`
- `src/main/resources/templates/index.html`
- `css/common.css`
- `css/top-page.css`
- `src/main/resources/static/css/common.css`
- `src/main/resources/static/css/top-page.css`
- `作業md/2026-03-25_1109_SPヘッダー調整とロゴ差し替え記録.md`
- `作業md/2026-03-24_1045_フロント修正依頼確認と対応方針メモ.md`

## 今回までの実施内容
### 1. ヘッダーロゴ差し替え
- PC / 中間幅では `img/TOP/roherarogo.png` を使用する形へ変更した。
- SP幅では `img/TOP/吟ロゴ.png` に切り替えるよう `picture` 要素で分岐した。

### 2. SPヘッダーの構造見直し
- `header-top` はロゴのみを持つ構造へ整理した。
- `header-actions` に `回答する`、`予約する`、ハンバーガーメニューをまとめた。
- SP幅では `ロゴ（左）` と `回答する / 予約する / ハンバーガー（右）` の 1 ロウ構成を前提に調整した。
- `header-inner` に残っていた `flex-direction: column` を解消し、`row` ベースで動くように修正した。

### 3. CTA 表示崩れの調整
- SP幅で CTA ラベルがはみ出す問題を調整した。
- ボタンの高さ、余白、フォントサイズ、`line-height` を見直した。
- `label-default` / `label-short` の同時表示で `回答する回答`、`予約する予約` になっていたため、通常SPでは `回答する` / `予約する`、狭幅だけ `回答` / `予約` に切り替えるルールを整理した。

### 4. ヘッダーナビ文言の復旧
- 構造調整時にヘッダーナビが英語 (`Flow` / `Question` / `Value` / `Info` / `FAQ` / `Reserve`) へ置き換わっていたため、日本語 (`流れ` / `質問` / `価値` / `情報` / `安心` / `お願い`) に復旧した。
- この件は重要インシデントとして別記録にも追記した。

### 5. 基本情報エリアの高さ調整
- `#information` の高さは左カラム単独ではなく、中央 / 右カラムを含めた 3 カラム全体の最も高い列に引っ張られる構造であることを確認した。
- 開発者ツール上で良かった状態に合わせ、`.info-middle, .info-right` の `display: grid;` を一度外した。
- その後、右カラムだけは `info-station` と地図を 1:1 で並べたい要件に合わせ、`.info-right` のみ `display: grid; grid-template-rows: 1fr 1fr;` に変更した。

## 現時点の見立て
- 基本情報エリアの高さは、左看板画像だけが原因ではない。
- 中央カラムの縦積み要素と、右カラムの駅画像 + Google Map が高さに強く影響している。
- そのため、セクション全体へ雑に `max-height` を入れるより、各カラムの高さ要因を個別に調整する方が安全。

## 完了扱いにしたこと
- [x] PC / 中間幅ロゴ差し替え
- [x] SP幅ロゴ差し替え
- [x] SPヘッダーの 1 ロウ化調整
- [x] CTA ラベル重複表示の解消
- [x] ヘッダーナビ文言の日本語復旧
- [x] 基本情報エリアの `.info-middle, .info-right` の `display: grid` 見直し
- [x] 右カラムの `info-station` と Google Map の 1:1 化

## 注意点
- PowerShell 上では日本語が文字化けして見えることがあるため、表示だけで文言を推定して置き換えない。
- 文言変更はレイアウト変更と別扱いで報告する。
- ヘッダーは `common.css` と `top-page.css` の責務が跨るので、どちらで直すかを先に整理する。
- 基本情報エリアは 3 カラムの合算で高さが決まるため、左画像だけを主因と決め打ちしない。

## 次に見る場合の観点
- `#information` へのアンカー遷移時に 1 画面でどこまで収まるか
- 地図 iframe の `min-height` をさらに下げる必要があるか
- 中央カラムの料金表ブロックの余白を詰める必要があるか
- 360px / 390px / 中間幅 / PC 幅でヘッダーと基本情報の再確認

## 追記: 下層ページヘッダーロゴ統一調整

### 対象ページ
- `questionnaire.html`
- `questionnaire_step2.html`
- `fragrance-graph.html`
- `reservation.html`
- `reservation-complete.html`
- Spring Boot 側の `src/main/resources/templates/*.html`

### 実施内容
#### 1. 下層ページの文字ロゴを画像ロゴへ統一
- 診断ページ、グラフページ、予約ページ、予約完了ページのヘッダーで、文字表示だった `FRAGRANCE WORKSHOP` を画像ロゴへ置き換えた。
- PC / 中間幅では `img/TOP/roherarogo.png` を使用。
- SP幅では `img/TOP/吟ロゴ.png` を使用するよう `picture` 要素で統一した。

#### 2. グラフページと予約ページのSP CTA幅調整
- `fragrance-graph.html` と `reservation.html` は、SP幅で CTA ボタンが広すぎてロゴを押し隠していた。
- そのため `header-cta` を内容幅ベースで収まるように調整した。
- `width: auto`、`max-width: calc(100% - 88px)`、`flex: 0 1 auto` を使い、ロゴ領域を確保した。

#### 3. 全ページのロゴ位置ずれ調整
- 当初はページごとにロゴ画像サイズ差が原因だと見ていたが、主因は各ページのSP用CSSで `header-inner` の幅や高さを個別上書きしていたことだった。
- `questionnaire-common.css`、`fragrance-graph.css`、`reservation.css`、`reservation-complete.css` のSP用 `header-inner` 幅上書きを外し、共通の `site-container` 幅へ戻した。
- 共通CSS側の `.brand` は `inline-flex` と `align-items: center` を明示し、ロゴの縦位置基準を固定した。

### 途中で認識違いを起こした点
#### 認識違い 1: ロゴ位置ずれは画像サイズ差が主因だと見た
- 実際にはロゴ画像サイズそのものより、各ページごとの `header-inner` 幅上書きとSPヘッダー高さルールの差が主因だった。
- 特に予約ページ系では、トップページと違うヘッダー高さルールが入っていたため、視覚上の中央がずれていた。

#### 認識違い 2: 視覚ずれをピクセル移動で補正しようとした
- 一時的に `transform: translateY(2px)` で予約系ロゴの位置補正を試した。
- これは根本原因への対処ではなく、結果として改善も安定しなかった。
- 最終的には個別の `min-height`、`padding-top`、`padding-bottom`、`transform` を外し、共通ヘッダー基準に戻して解消した。

#### 認識違い 3: reservation.html にロゴが未反映だと判断した
- 実際には `reservation.html` にロゴ画像は反映済みで、CTAボタンが広すぎてロゴが見えにくい状態だった。
- 「未反映」と見えたのは、表示上の圧迫を構造不備と誤認したため。

### 今回の最終整理
- トップページのSPロゴ見え方を基準とし、他ページをその基準へ寄せた。
- 各ページ個別CSSでは、必要なCTA幅調整だけ残し、ロゴ位置やヘッダー高さは共通CSS基準へ戻した。
- 予約ページ / 完了ページで入れていた個別の `min-height: 56px`、`padding-top: 0`、`padding-bottom: 0`、`transform` 補正は撤去した。

### 今後の注意点
- ロゴ位置ずれを見た時は、まず画像サイズではなく `header-inner`、`page-header-inner`、`header-cta` の上書き差分を確認する。
- 視覚上のずれを `translateY()` などのピクセル補正で先に直さない。必ず構造・余白・高さの差分を先に潰す。
- 「表示されていない」と「押し出されて見えない」を切り分ける。HTML反映有無とレイアウト圧迫は別問題として扱う。
- 共通ヘッダーの見え方を変える場合は、トップページを基準ページとして、他ページとの差分を一覧で確認してから編集する。

### 今回更新した主なファイル
- `css/common.css`
- `css/top-page.css`
- `css/questionnaire-common.css`
- `css/fragrance-graph.css`
- `css/reservation.css`
- `css/reservation-complete.css`
- `src/main/resources/static/css/common.css`
- `src/main/resources/static/css/top-page.css`
- `src/main/resources/static/css/questionnaire-common.css`
- `src/main/resources/static/css/fragrance-graph.css`
- `src/main/resources/static/css/reservation.css`
- `src/main/resources/static/css/reservation-complete.css`
- `questionnaire.html`
- `questionnaire_step2.html`
- `fragrance-graph.html`
- `reservation.html`
- `reservation-complete.html`
- `src/main/resources/templates/questionnaire.html`
- `src/main/resources/templates/questionnaire_step2.html`
- `src/main/resources/templates/fragrance-graph.html`
- `src/main/resources/templates/reservation.html`
- `src/main/resources/templates/reservation-complete.html`
## 追記: 各ページヘッダーロゴ最終統一と認識差分の記録

### 対象
- トップページ
- questionnaire.html
- questionnaire_step2.html
- fragrance-graph.html
- 
reservation.html
- 
reservation-complete.html
- 上記に対応する src/main/resources/templates/*.html
- 上記に対応する src/main/resources/static/css/*.css

### 今回の最終到達点
- 各ページのヘッダーロゴサイズは、SP幅では questionnaire.html / questionnaire_step2.html の見え方を基準に揃えた。
- トップページも同基準へ戻し、SP時に小さく見えすぎない状態へ調整した。
- fragrance-graph.html と 
reservation.html は、CTAボタンがロゴを押し隠さないよう、CTA幅のみ個別調整した。
- 
reservation.html / 
reservation-complete.html のロゴ縦位置は、個別補正ではなく、トップページ基準の共通ヘッダー構造へ戻すことで揃えた。

### 実施内容
#### 1. 下層ページの文字ロゴを画像ロゴへ統一
- 診断ページ、グラフページ、予約ページ、予約完了ページで、文字表示のヘッダーを画像ロゴへ置き換えた。
- PC / 中間幅は img/TOP/roherarogo.png、SP幅は img/TOP/吟ロゴ.png を使用するよう統一した。

#### 2. グラフページと予約ページのSP CTA幅調整
- fragrance-graph.html と 
reservation.html は、SP時にCTAが広すぎてロゴ領域を圧迫していた。
- header-cta を width: auto、max-width: calc(100% - 88px)、flex: 0 1 auto ベースへ調整し、ロゴが見える幅を確保した。

#### 3. 各ページのロゴ位置ずれの主因を構造差分として整理
- 当初はロゴ画像サイズ差が主因と見ていたが、実際には各ページごとの header-inner 幅上書き、SP時のヘッダー高さルール差分が位置ずれの主因だった。
- questionnaire-common.css、fragrance-graph.css、
reservation.css、
reservation-complete.css のSP用 header-inner 幅上書きを外し、共通の site-container 幅へ戻した。
- 共通CSS側では .brand を inline-flex と align-items: center に整理し、ロゴの縦位置基準を固定した。

#### 4. 予約ページ系の縦位置差分を共通構造へ戻して解消
- 
reservation.html / 
reservation-complete.html では、SP時にトップページと異なる min-height、padding-top、padding-bottom が入っていた。
- この差分が、ロゴが上寄りに見える原因だった。
- 最終的に、個別の高さ補正やピクセル補正は撤去し、共通ヘッダー基準へ戻した。

### 途中で起きた認識違い
#### 認識違い 1: ロゴ位置ずれを画像サイズ差の問題として先に見た
- 実際の主因は、各ページCSSの header-inner とヘッダー高さルールの差分だった。
- 画像サイズだけを見ていると、構造差分の確認が遅れる。

#### 認識違い 2: 視覚差分をピクセル補正で解決しようとした
- 一時的に transform: translateY(2px) による補正を試した。
- これは構造差分を隠すだけで、根本解決にならず、改善も安定しなかった。
- 今後同様の事象では、translateY() などの微調整は最後の手段とし、先に min-height、padding、align-items、ラッパー構造差分を確認すること。

#### 認識違い 3: 
reservation.html はロゴ未反映と判断した
- 実際にはロゴは反映済みで、CTAボタンの横幅が広すぎたため、ロゴが押し出されて見えにくくなっていた。
- HTML反映有無と、レイアウト圧迫による視認性低下は分けて判断する必要がある。

### 今後の対策・留意点
- ヘッダー位置ずれが出た場合は、まず common.css 基準と各ページ個別CSSの差分を比較する。
- ロゴサイズを触る前に、header-inner、page-header-inner、.brand、.header-cta、ラッパー要素の width / min-height / padding を確認する。
- 見た目の差分をピクセル移動で合わせない。構造・余白・高さが揃っているかを先に確認する。
- 「ロゴがない」のか「CTAに押されて見えない」のかを切り分ける。DOM反映とレイアウト圧迫は別問題として扱う。
- ヘッダー改修時はトップページを基準画面として、他ページとの差分比較を前提に作業する。

### 補足
- 今回のロゴ位置調整は、トップページで確認済みの見え方を基準に各ページを寄せる方針で実施した。
- そのため、今後さらにヘッダーを調整する場合も、トップページのヘッダー構造・余白・ロゴ位置を基準値として扱うこと。
## 追記: reservation / reservation-complete の表示改善

### 1. reservation.html の予約枠選択状態表示修正
- eservation.html では、予約枠を選択しても上部の状態表示が 未選択: 来店日時を選んでください のまま残る不整合があった。
- 原因は、状態表示更新が「日時選択」と「人数選択」の両方を満たした場合にしか切り替わらない実装だったこと。
- フロント側の updateSelectionState() を見直し、バックエンド未実装でも以下の表示になるよう修正した。
  - 日時未選択: 未選択: 来店日時を選んでください
  - 日時のみ選択: 選択中: 選択した日時 / 人数を選択してください
  - 日時と人数を選択: 選択中: 選択した日時 / 選択人数
- これにより、予約枠の視覚状態と文言状態を一致させた。

### 2. reservation-complete.html に地図導線を追加
- 完了画面の hero-card glass-panel に、来店場所への導線を追加した。
- スマホでは地図導線ボタンをタップできる構成、PCではQRコードも見える構成を前提に整理した。

#### 実施内容
- 開催場所の住所を右側導線ブロックとして表示。
- トップページの地図へ ボタンを追加し、トップページの基本情報エリア内のGoogle Map位置へ遷移できるようにした。
- Googleマップを開く ボタンを追加し、外部Google Mapへ直接飛べるようにした。
- PC用QRコードは外部生成APIを使わず、img/TOP/グーグルマップ地図上野教室.png を利用する形へ変更した。
- トップページ側では地図カードに id="information-map" を付与し、完了画面からアンカー遷移できるようにした。

### 3. reservation-complete.html のPCレイアウト再整理
- 初回の配置では、QRコードとCTAが縦積みになり、右側ブロックが意図より大きく見えていた。
- そのためPC版では、右側を以下の構成へ組み替えた。
  - 左: QRコード
  - 右: トップページの地図へ / Googleマップを開く のCTAボタン群
- あわせて detail-row の比率を見直し、detail-card glass-panel をより広く、ction-card glass-panel を細めに再配分した。
- SP幅では従来どおり縦積みになるよう維持した。

### 4. 今回確認したこと・留意点
- 予約ページ関連の作業前に、作業md/3-13まで/2026-03-11_1555_予約ページ_reservation引継ぎメモ.md と 作業md/2026-03-24_1045_フロント修正依頼確認と対応方針メモ.md を確認した。
- 2026-03-24_1045_フロント修正依頼確認と対応方針メモ.md では、以下のチェック項目を今回完了扱いへ更新した。
  - 予約枠選択済みなのに「来店日時を選んでください」のまま残る文言不整合を解消する。
  - 選択済み状態の視覚表示と文言表示を一致させる。
  - 予約枠選択済みでも文言が「来店日時を選んでください」のまま残る問題の解消

### 5. 今後の注意点
- eservation.html の状態表示は、バックエンド接続前でもフロント単体で選択状態が分かることを優先する。
- 予約完了画面の地図導線は、スマホとPCで必要な見え方が異なるため、同じ要素を単純流用せず、SP/PCの役割を分けて設計する。
- 外部サービス依存で代替可能な部分でも、クライアント指定の画像資産がある場合はそちらを優先する。
- 完了画面のレイアウト変更では、hero-card 単体だけでなく、直下の detail-row との面積バランスも合わせて確認する。
## 2026-03-26 時点の完了整理
- 3/24 と 3/25 に実施したフロント調整は、問い合わせフォームを除き完了済みとして扱う。
- 問い合わせフォームは Must ではなく、時間的余裕がある場合の追加要件とする。
- 後任者は、このファイルと 2026-03-24_1045_フロント修正依頼確認と対応方針メモ.md を見れば、完了範囲と保留範囲を判別できる状態とする。