# 引継ぎメモ: トップページとアンケート調整状況

## 対象

- `香りワークショップ_トップページ.html`（現 `top-page.html`）
- `questionnaire.html`
- `香りワークショップトップページ_レスポンシブ対応.md`

## 1. トップページの作業内容

### 状態

- PC版は完成扱い
- レスポンシブ対応も完成扱い
- 最小幅は `375px`

### 実施済み内容

- `html` / `body` に `min-width: 375px`
- ブレークポイントを `PC / 中間帯 / SP` の3段階に整理
- `16:9` 条件だけでPC化する挙動を撤廃
- 中間帯専用レイアウトを追加
- SP専用のハンバーガーメニューを追加
- `hero-gallery` はSPで非表示
- SPでは `hero-visual` を短くし、中央表示に調整
- `reassurance` のCTA文言と配置を調整
- `Final Call To Action` セクションの見出し・CTA配置を調整
- `value-visual-secondary` の画像を `img/TOP/Workshop体験.png` に変更
- 同画像の曇りエフェクトを削除

### 補足

- トップページのレスポンシブ詳細は `香りワークショップトップページ_レスポンシブ対応.md` に記録済み

## 2. questionnaire.html の作業内容

### 方針

- トップページと同じく `PC / 中間帯 / SP` の3段階で調整
- ヘッダーの `前の質問 / 次の質問` という設計意図は維持
- 右サイドのはみ出し修正を優先

### 実施済み内容

- `html` / `body` に `min-width: 375px`
- `question-shell > * { min-width: 0; }` を追加
- ブレークポイントを以下に整理
  - PC: `min-width: 68.75em`
  - 中間帯: `min-width: 48em and max-width: 68.6875em`
  - SP: `max-width: 47.9375em`

### PC

- 右サイドはみ出し対策として、PC時の `question-shell` を割合指定から安全幅寄りの3カラムへ変更
  - 左: `minmax(190px, 220px)`
  - 中央: `minmax(0, 1fr)`
  - 右: `minmax(260px, 292px)`
- `side-panel` / `store-visual` / `side-card` / `return-card` に `min-width: 0`
- 右サイド本文に `overflow-wrap: anywhere`
- `action-btn` に `max-width: 100%`

### 中間帯

- `question-shell` は1カラム化
- `nav-panel` は非表示
- `step-strip` は非表示
- `main-panel` を上段に配置
- `side-panel` を下段フル幅に配置
- `side-panel` は横3カラム化
- 配置順は中間帯専用で以下
  - `return-card`
  - `side-card`
  - `store-visual`

### スマホ

- `question-shell` を1カラム化
- `nav-panel` は非表示ではなく、横スクロール式の質問ナビに変更
- `step-strip` は縦積みに変更
- `question-title`、`option-card`、`sub-options` などをSP向けに圧縮
- `scroll-padding-top` を `96px` に変更

### そのほか

- `noteTitle` は `豆知識＜...＞` の表示を `豆知識` と `＜...＞` の2行表示に変更
  - 実装は `question.noteTitle.replace("＜", "<br>＜")`

## 3. 現在の注意点

### questionnaire.html

- 中間帯はかなり大きく再設計済みだが、まだ実画面確認ベースの微調整が残る可能性あり
- SPでは `nav-panel` を横スクロール表示にしているため、実機での押しやすさ確認が必要
- `side-panel` の中間帯レイアウトは、依頼どおり「配置を逆」にしてある
  - 画像イメージ通りにしたい場合は、再度並び順確認が必要

## 4. 次にすべきこと

### 優先度高

- `questionnaire.html` の中間帯表示を実画面ベースで確認
  - `main-panel` の高さ感
  - `side-panel` の3カラムの収まり
  - `return-card` / `side-card` / `store-visual` の並びが意図通りか

- `questionnaire.html` のSP表示を確認
  - 横スクロールナビの押しやすさ
  - 選択肢カードの縦余白
  - ヘッダーボタンの収まり

### 次の候補

- `questionnaire_step2.html` に同じレスポンシブ方針を展開
- `questionnaire.html` の調整内容も Markdown に別途まとめる

## 5. 引継ぎ時の理解ポイント

- トップページは完成扱いなので、原則は大きく触らない
- 現在の主戦場は `questionnaire.html`
- `questionnaire.html` は「ヘッダー構造はそのまま」「レスポンシブだけ再設計」が前提
- 次の作業は、コード追加よりも実表示に合わせた微修正フェーズに入っている
